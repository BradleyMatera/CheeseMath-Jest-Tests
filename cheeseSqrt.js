// Finds the square root of a number, representing slices of cheese.
const cheeseSqrt = (a) => {
  console.log(`Calculating the square root of ${a} slices of cheese.`);
  return Math.sqrt(a);
};

module.exports = cheeseSqrt;